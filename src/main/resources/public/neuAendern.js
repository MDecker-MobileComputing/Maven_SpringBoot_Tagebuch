"use strict";

/**
 * Event-Handler-Funktion für Klick auf den "Speichern"-Button.
 *
 * @returns {Boolean} false, um Laden einer anderen Seite zu verhindern.
 */
function onSpeichernButton() {

    alert("Speichern ist noch nicht implementiert");

    return false;
}
