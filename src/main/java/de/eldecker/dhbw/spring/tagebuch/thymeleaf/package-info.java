/**
 * Dieses Paket enthält Controller (nicht RestController!) für die Thymeleaf-Views.
 * Thymeleaf ist eine sog. Template-Engine: HTML-Seiten werden also auf Basis
 * von Vorlagen (Templates) erzeugt, wobei die Templates Platzhalterwerte
 * enthalten können, die vor der Anzeige ersetzt werden.
 * Es ist auch möglich, Schleifen oder bedingte Anzeige von Inhalten zu realisieren.
 * <br><br>
 *
 * Siehe auch die Konfigurationswerte in der Datei {@code application.properties}.
 */
package de.eldecker.dhbw.spring.tagebuch.thymeleaf;